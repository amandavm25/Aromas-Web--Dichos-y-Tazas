@model AromasWeb.Abstracciones.ModeloUI.SolicitudVacaciones

@{
    ViewBag.Title = "Editar Solicitud de Vacaciones";
}

<!-- Hero Section -->
<section class="hero" style="min-height: 40vh; padding: 8rem 0 4rem;">
    <div class="hero-bg-overlay"></div>
    <div class="hero-leaves"></div>
    <div class="container hero-container" style="display: block;">
        <div class="hero-content" data-aos="fade-up" style="text-align: center;">
            <span class="hero-badge">
                <i class="fas fa-edit"></i>
                Actualizar Información
            </span>
            <h1 class="hero-title" style="font-size: 3.5rem;">
                Editar
                <span class="hero-title-accent">Solicitud</span>
            </h1>
            <p class="hero-subtitle">
                Modifica los datos de la solicitud de vacaciones
            </p>
        </div>
    </div>
    <div class="scroll-indicator">
        <div class="mouse"><div class="wheel"></div></div>
    </div>
</section>

<!-- Formulario Section -->
<section class="admin-form-section">
    <div class="container" style="max-width: 80%;">
        @using (Html.BeginForm("EditarSolicitud", "SolicitudVacaciones", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(model => model.IdSolicitud)
            @Html.HiddenFor(model => model.IdEmpleado)
            @Html.HiddenFor(model => model.Estado)

            <div class="admin-form-wrapper" data-aos="fade-up">
                @Html.ValidationSummary(true, "Por favor, corrige los siguientes errores:", new { @class = "alert alert-danger" })

                <!-- Sección Empleado -->
                <div class="admin-form-header">
                    <i class="fas fa-user-tie"></i>
                    <h5>Información del Empleado</h5>
                </div>

                <div style="margin: 0 0 1.5rem; padding: 1.5rem; background: var(--cream); border-radius: 20px; border-left: 5px solid var(--olive-green);">
                    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 1.5rem;">
                        <div>
                            <small style="color: var(--charcoal); opacity: 0.7; font-size: 0.8rem; font-weight: 600; text-transform: uppercase; display: block; margin-bottom: 0.3rem;">
                                <i class="fas fa-user" style="color: var(--olive-green);"></i> Empleado
                            </small>
                            <strong style="color: var(--dark-green);">@Model.NombreEmpleado</strong>
                        </div>
                        <div>
                            <small style="color: var(--charcoal); opacity: 0.7; font-size: 0.8rem; font-weight: 600; text-transform: uppercase; display: block; margin-bottom: 0.3rem;">
                                <i class="fas fa-briefcase" style="color: var(--olive-green);"></i> Cargo
                            </small>
                            <strong style="color: var(--dark-green);">@ViewBag.CargoEmpleado</strong>
                        </div>
                        <div>
                            <small style="color: var(--charcoal); opacity: 0.7; font-size: 0.8rem; font-weight: 600; text-transform: uppercase; display: block; margin-bottom: 0.3rem;">
                                <i class="fas fa-calendar-plus" style="color: var(--olive-green);"></i> Contratación
                            </small>
                            <strong style="color: var(--dark-green);">@Model.FechaContratacionEmpleado.ToString("dd/MM/yyyy")</strong>
                        </div>
                        <div>
                            <small style="color: var(--charcoal); opacity: 0.7; font-size: 0.8rem; font-weight: 600; text-transform: uppercase; display: block; margin-bottom: 0.3rem;">
                                <i class="fas fa-calendar-check" style="color: var(--gold-bright);"></i> Días Disponibles
                            </small>
                            <strong style="color: var(--gold-bright); font-size: 1.3rem;">@Model.DiasDisponibles días</strong>
                        </div>
                    </div>
                    @Html.HiddenFor(model => model.DiasDisponibles)
                </div>

                <div class="form-group">
                    <label><i class="fas fa-calendar"></i> Fecha de Solicitud</label>
                    @Html.EditorFor(model => model.FechaSolicitud, new { htmlAttributes = new { @class = "form-control", type = "date", @readonly = "readonly" } })
                    @Html.ValidationMessageFor(model => model.FechaSolicitud, "", new { @class = "text-danger" })
                    <small style="display: block; margin-top: 0.5rem; color: var(--charcoal); opacity: 0.7;">
                        <i class="fas fa-info-circle" style="color: var(--gold-bright);"></i> Fecha original de la solicitud
                    </small>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label><i class="fas fa-play"></i> Fecha de Inicio</label>
                        @Html.EditorFor(model => model.FechaInicio, new { htmlAttributes = new { @class = "form-control", type = "date" } })
                        @Html.ValidationMessageFor(model => model.FechaInicio, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-stop"></i> Fecha de Fin</label>
                        @Html.EditorFor(model => model.FechaFin, new { htmlAttributes = new { @class = "form-control", type = "date" } })
                        @Html.ValidationMessageFor(model => model.FechaFin, "", new { @class = "text-danger" })
                    </div>
                </div>

                <!-- Días calculados -->
                <div class="form-group">
                    <label><i class="fas fa-calculator"></i> Días Laborables</label>
                    <div style="display: flex; align-items: center; gap: 1rem; background: var(--cream); border-radius: 15px; padding: 0.8rem 1.2rem;">
                        <span style="color: var(--charcoal); opacity: 0.7; font-size: 0.9rem;">Calculados automáticamente:</span>
                        <span id="diasCalculados" style="background: var(--olive-green); color: white; font-size: 1.1rem; font-weight: 700; padding: 0.3rem 1rem; border-radius: 50px;">@Model.DiasSolicitados</span>
                        <span style="color: var(--charcoal); opacity: 0.7; font-size: 0.85rem;">días (excluye domingos)</span>
                    </div>
                    @Html.HiddenFor(model => model.DiasSolicitados)
                </div>

                <!-- Alerta días insuficientes -->
                <div id="alertaDias" style="display: none; background: rgba(231,76,60,0.1); padding: 1rem 1.5rem; border-radius: 15px; margin: 0 0 1.5rem; border-left: 5px solid #e74c3c;">
                    <p style="margin: 0; color: #c0392b; font-weight: 600; display: flex; align-items: center; gap: 0.7rem;">
                        <i class="fas fa-exclamation-triangle"></i>
                        <span id="mensajeAlerta"></span>
                    </p>
                </div>

                <div style="margin: 0 0 1.5rem; padding: 1.5rem; background: var(--cream); border-radius: 20px;">
                    <div style="display: flex; align-items: center; gap: 2rem; flex-wrap: wrap;">
                        <div>
                            <small style="color: var(--charcoal); opacity: 0.7; font-size: 0.8rem; font-weight: 600; text-transform: uppercase; display: block; margin-bottom: 0.3rem;">
                                <i class="fas fa-hashtag" style="color: var(--olive-green);"></i> ID Solicitud
                            </small>
                            <strong style="color: var(--dark-green);">#@Model.IdSolicitud</strong>
                        </div>
                        <div>
                            <small style="color: var(--charcoal); opacity: 0.7; font-size: 0.8rem; font-weight: 600; text-transform: uppercase; display: block; margin-bottom: 0.3rem;">
                                <i class="fas fa-info-circle" style="color: var(--olive-green);"></i> Estado Actual
                            </small>
                            <span style="background: @Model.EstadoBadgeColor; color: white; padding: 0.35rem 1rem; border-radius: 50px; font-size: 0.85rem; font-weight: 600; display: inline-block;">
                                @Model.Estado
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Botones -->
                <div class="admin-form-footer">
                    <button type="button" class="btn btn-secondary" onclick="window.location.href='@Url.Action("ListadoSolicitudes", "SolicitudVacaciones")'">
                        <span><i class="fas fa-times"></i> Cancelar</span>
                    </button>
                    <button type="submit" class="btn btn-primary btn-3d" id="btnGuardar">
                        <span><i class="fas fa-check"></i> Guardar Cambios</span>
                    </button>
                </div>
            </div>
        }
    </div>
</section>

@section Scripts {
    <script src="~/js/solicitudvacaciones.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const hiddenDias = document.querySelector('input[name="DiasDisponibles"]');
            const alerta = document.getElementById('alertaDias');
            const btn = document.getElementById('btnGuardar');

            function validarDias() {
                const solicitados = parseInt(document.getElementById('diasCalculados').textContent) || 0;
                const disponibles = parseFloat(hiddenDias.value || '0');
                if (solicitados > disponibles) {
                    document.getElementById('mensajeAlerta').textContent = `Días insuficientes. Solicitados: ${solicitados}, Disponibles: ${disponibles}`;
                    alerta.style.display = 'block';
                    btn.disabled = true; btn.style.opacity = '0.5'; btn.style.cursor = 'not-allowed';
                } else {
                    alerta.style.display = 'none';
                    btn.disabled = false; btn.style.opacity = '1'; btn.style.cursor = 'pointer';
                }
            }

            new MutationObserver(validarDias).observe(
                document.getElementById('diasCalculados'),
                { childList: true, characterData: true, subtree: true }
            );
            validarDias();
        });
    </script>
}